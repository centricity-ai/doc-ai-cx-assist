source "https://rubygems.org"

# Lock to GitHub Pages gem set for compatibility with GH Pages build
gem "github-pages", group: :jekyll_plugins

# Optional: explicitly add jekyll (github-pages includes a pinned version)
# gem "jekyll", "~> 3.9"